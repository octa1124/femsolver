# FEniCSx Tooling

This directory is reserved for canonical FEniCSx validation cases and weak-form cross-check scripts.

`FEniCSx` is treated as a reference and verification tool, not as the production solver kernel.
