# Contract Tests

This directory is reserved for schema and interface-contract tests. The initial repository baseline keeps contract coverage in unit and Python tooling tests until the first concrete case schema lands.
