# Integration Tests

This directory is reserved for end-to-end application tests. The current baseline registers smoke-style CTest integration checks for `motor_pre`, `motor_solve`, and `motor_check`.
